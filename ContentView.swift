import SwiftUI

struct ContentView: View {
    @State var number1 = ""
    @State var number2 = ""
    @State var answer = ""
    var body: some View {
        VStack {
            Text("Quotes4U")
        }
    }
}
